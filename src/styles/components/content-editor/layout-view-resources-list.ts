import { createStyles } from "@material-ui/core";

export default createStyles({

  list: {},

  listItem: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center"
  },

  listItemTitle: {},
  listItemSubtitle: {}

});
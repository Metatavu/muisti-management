import { createStyles } from "@material-ui/core";

export default createStyles({

  container: {
    display: "flex"
  },

  text: {
    marginTop: 25,
    display: "flex",
    justifyContent: "center",
    flexGrow: 1
  }

});
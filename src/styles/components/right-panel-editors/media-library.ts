import { createStyles } from "@material-ui/core";
import theme from "../../theme";

export default createStyles({
  root: {
    display: "block"
  },
  folder: {
    display: "flex",
    alignItems: "center"
  },
  mediaThumbnail: {
    borderRadius: 0
  }
});

import { createStyles } from "@material-ui/core";

export default createStyles({

  root: {
    gridArea: "view-selection",
    backgroundColor: "#FBFBFB",
    borderRight: "1px solid #ddd"
  },

  buttonGroup: {
    display: "flex",
    flexDirection: "column"
  }

});
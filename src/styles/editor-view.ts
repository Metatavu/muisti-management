import { createStyles } from "@material-ui/core";

export default createStyles({

  root: {
    gridArea: "editor-view",
    backgroundColor: "#eee",
    overflowY: "initial",
    overflowX: "auto"
  }

});